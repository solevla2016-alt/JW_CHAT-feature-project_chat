services:
  - type: web
    name: jwchat-backend
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile.backend
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: jwchat-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: jwchat-redis
          property: connectionString
      - key: CORS_ALLOWED_ORIGINS
        value: "https://jwchat.vercel.app"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://jwchat.vercel.app"

  - type: redis
    name: jwchat-redis
    plan: free
    maxmemoryPolicy: noeviction

databases:
  - name: jwchat-db
    plan: free
